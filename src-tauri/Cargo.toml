[package]
name = "newt"
version = "0.0.0"
description = "A Tauri App"
authors = ["you"]
license = ""
repository = ""
edition = "2021"

[build-dependencies]
tauri-build = { version = "1.3", features = [] }

[dependencies]
tauri = { version = "1.3", features = ["dialog-ask", "dialog-confirm", "dialog-message", "shell-open"] }
serde = { version = "1.0", features = ["derive"] }
thiserror = "1"
anyhow = "1"
serde_json = "1.0"
opener = "0.6"
once_cell = "1.17"
notify = "6.0"
uuid = "1.3"
url = "2.3"
regex = "1.8"
arboard = "3.2"
parking_lot = "0.12"
treediff = { version = "4", features = ["with-serde-json"] }

[target.'cfg(target_os = "linux")'.dependencies]
webkit2gtk = "*"

[features]
# this feature is used for production builds or when `devPath` points to the filesystem
# DO NOT REMOVE!!
custom-protocol = ["tauri/custom-protocol"]
