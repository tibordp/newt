[package]
name = "pty-process"
version = "0.3.0"
edition = "2024"
license = "MIT"

[dependencies]
libc = "*"
nix = { version = "0.31", features = ["fs", "ioctl", "process", "term"] }
tokio = { version = "*", features = ["fs", "process", "net"] }
